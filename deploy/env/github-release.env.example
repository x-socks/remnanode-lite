# Pull-based updates from GitHub releases.
# `RUNTIME_VERSION=latest` follows the newest published runtime.
# Set a concrete upstream version to pin upgrades to that release.

REPO_SLUG=owner/repo
RUNTIME_VERSION=latest
BASE_DIR=/opt/remnanode
RESTART_SERVICE=0
